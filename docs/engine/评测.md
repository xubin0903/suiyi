# 评测

`scripts/eval_samples.py` 用固定样例集给当前模型打分，供模型选型复核和以后每次改动做回归。它直接调用 `suiyi_engine.translator.Translator`，不经过 HTTP。翻译核心本身不在这里修改；效果问题另开 Issue。

样例集是 [tests/samples/zh_core_v1.jsonl](../../tests/samples/zh_core_v1.jsonl)（见该目录 README）。参考译文目前都是 `draft`。

## 安装

评测依赖在可选组 `eval`，不进运行时，也不进只做翻译的安装：

```bash
pip install -e "engine[eval]"
```

组里是 `sacrebleu` 和 `psutil`。日文 BLEU 若要使用 `ja-mecab`，再装 sacrebleu 的可选分词（其依赖通常包含 MeCab）：

```bash
pip install "sacrebleu[ja]"
```

等价的常见包是 `mecab-python3` 与 `unidic-lite`。没装、或装了但词典不可用时，脚本不会失败，日文 BLEU 回退 `tokenize=char`，并在报告的环境一节写明原因。CI 只安装 `engine[dev,eval]`，不安装 MeCab，也不下载模型。

## 命令

在仓库根目录执行。模型需先按 [模型目录约定](模型目录约定.md) 转成 CTranslate2 int8。

```bash
python scripts/eval_samples.py \
  --samples tests/samples/zh_core_v1.jsonl \
  --models-dir models \
  --out reports/eval-2026-09-25
```

只跑部分方向，或每个方向只取文件里的前 N 条：

```bash
python scripts/eval_samples.py \
  --samples tests/samples/zh_core_v1.jsonl \
  --models-dir models \
  --out reports/eval-zh-en \
  --directions zh-en,en-zh \
  --limit 5
```

| 参数 | 含义 |
| --- | --- |
| `--samples` | JSONL 样例集 |
| `--models-dir` | 已转换模型的根目录 |
| `--out` | 输出目录 |
| `--directions` | 逗号分隔，如 `zh-en,ja-zh`。也接受 `zh→en`。默认跑样例里出现的全部方向 |
| `--limit` | 每个方向最多评测前 N 条（文件顺序）。被截掉的 `clean_id` 不会计入 OCR 差值 |
| `--seed` | 对照样例的随机种子，默认 `0` |

方向顺序固定为 zh→en、en→zh、zh→ja、ja→zh、en→ja、ja→en，然后才是样例里其余方向。

每个方向先 `preload`（中转会加载两段模型），再逐条 `translate`。加载耗时单列；P50 / P95 / max 用的是预热之后的 `elapsed_ms`，不含这次预热。前面方向已经加载过的模型，后面方向的加载耗时会接近 0。

退出码：`0` 成功，`1` 翻译或样例内容错误，`2` 参数或路径错误。

## 输出

`reports/` 已写入根目录 `.gitignore`。不要把生成的 `report.md`、`results.jsonl`、`summary.json` 提交进 git。需要留档的基线表写在本文「基线结果」。

| 文件 | 内容 |
| --- | --- |
| `report.md` | 环境、模型 revision、总表、分方向 × 分类、专名未保留清单、每方向最多 5 条对照 |
| `results.jsonl` | 每条样例的译文、`route`、`route_label`、耗时、chrF、BLEU、专名命中、OCR 差值 |
| `summary.json` | 汇总指标（`schema_version` 为 1），便于以后做回归阈值比对 |

`report.md` 的环境信息包括 CPU 型号、核数、内存、操作系统、Python、ctranslate2、sacrebleu、psutil、引擎版本，以及各模型 `suiyi-model.json` 里的 `hf_revision`。峰值 RSS 是进程高水位：Linux 读 `/proc/self/status` 的 `VmHWM`，Windows 用 `peak_wset`。

中转方向的 route 写成语种路径。ja→zh 没有直连模型，报告中为 `ja→en→zh`，并附上 `opus-mt-ja-en` 与 `opus-mt-en-zh`。

## 指标

分数都来自 sacrebleu，标度是 **0–100**（不是模型卡上有时见到的 0–1 chrF）。

| 指标 | 算法 |
| --- | --- |
| chrF | 主指标。`CHRF()` 默认：去掉空白后的字符 n-gram，`word_order=0`（chrF2）。中文和日文因此按字符计 |
| BLEU | 语料级 `BLEU`。目标语为中文时 `tokenize=zh`，日文为 `ja-mecab` 或回退 `char`，其余为 `13a` |
| 专名保留率 | `must_keep` 里每一项的 `tgt` 是否作为连续子串出现在译文中。大小写敏感，不做 NFKC。空 `tgt` 不算保留。比例是全部项的微平均（保留项数 / 总项数），不是句子平均 |
| OCR chrF 差值 | 对 `ocr_noise` 及其 `clean_id`：`chrF(噪声译文, 参考) - chrF(干净译文, 参考)`，再对方向内取算术平均。负值表示噪声把译文拉离参考 |

chrF / BLEU 只统计参考译文去空白后非空的条目。汇总用的是语料级分数，不是句级分数的平均。`results.jsonl` 里另有句级 chrF / BLEU，方便看单条。

短句往往凑不齐 4-gram，语料级 BLEU 会偏低甚至接近 0。回归比较以 chrF 为主。

延迟的 P50 / P95 在排好序的耗时上做线性插值。`max` 是最大值。

不做 COMET，也不做人工评测流程。报告里的对照样例只供抽查。

## 测试

指标和编排的单元测试在 `engine/tests/test_eval_metrics.py`、`engine/tests/test_eval_runner.py`。路由、专名和 OCR 差值用假后端，不加载权重。

真实模型用 `@pytest.mark.model`：未设置 `SUIYI_MODELS_DIR` 或目录不存在时跳过，CI 不下载模型。

```bash
ruff check engine
ruff format --check engine
pytest engine
```

## 基线结果

2026-09-25 在本机用清单里的五个 mvp 模型（int8）跑完 `zh_core_v1` 全部 132 条。命令：

```bash
python scripts/eval_samples.py \
  --samples tests/samples/zh_core_v1.jsonl \
  --models-dir models \
  --out reports/eval-2026-09-25
```

`reports/` 不进 git。下面是当次 `report.md` 的环境、总表和分方向表。样例 SHA256 为 `e588d705826cc3c042d6a25eab6f9049824b05aa0f13f37df3cc28e4f3258e86`。评测时间 `2026-09-25T09:12:19+00:00`。

| 项目 | 值 |
| --- | --- |
| CPU | Intel(R) Xeon(R) Processor |
| 核数 | 4 |
| 内存 | 15.6 GiB（16791945216 字节） |
| 操作系统 | Linux-6.12.94+-x86_64-with-glibc2.39 |
| Python | 3.12.3 |
| ctranslate2 | 4.8.2 |
| sacrebleu | 2.6.0 |
| 日文 BLEU | `char`（本机没有可用的 ja-mecab） |
| 峰值 RSS | 773.4 MiB（810979328 字节） |

| 模型 id | hf_revision | 量化 |
| --- | --- | --- |
| opus-mt-zh-en | cf109095479db38d6df799875e34039d4938aaa6 | int8 |
| opus-mt-en-zh | 408d9bc410a388e1d9aef112a2daba955b945255 | int8 |
| opus-mt-tc-big-zh-ja | d621a8794dc9f9477b6e74e2fead2746a39ea999 | int8 |
| opus-mt-ja-en | 0770961a39ba6bd66305b149c3f4110bcafca2e6 | int8 |
| opus-tatoeba-en-ja | 3a282648cb991174f3c423e376aff3a13e5edaaf | int8 |

ja→zh 是中转，route 为 `ja→en→zh`（`opus-mt-ja-en` → `opus-mt-en-zh`）。ja→en 的加载耗时为 0，因为中转已经加载过 `opus-mt-ja-en`。延迟是预热之后的单条耗时。

### 总表

| 方向 | route | 条数 | chrF | BLEU | 专名保留率 | OCR chrF 差值 | 加载（ms） | P50（ms） | P95（ms） | max（ms） |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| zh→en | zh→en（opus-mt-zh-en） | 31 | 54.65 | 29.34 | 15/24（62.5%） | -4.73（n=6） | 154.7 | 36.2 | 69.3 | 118.3 |
| en→zh | en→zh（opus-mt-en-zh） | 31 | 28.68 | 32.08 | 12/16（75.0%） | -3.83（n=6） | 149.7 | 23.6 | 56.1 | 67.3 |
| zh→ja | zh→ja（opus-mt-tc-big-zh-ja） | 25 | 1.61 | 0.42 | 0/12（0.0%） | +0.48（n=5） | 321.2 | 32.1 | 64.2 | 87.5 |
| ja→zh | ja→en→zh（opus-mt-ja-en → opus-mt-en-zh） | 25 | 24.72 | 26.06 | 6/12（50.0%） | -0.16（n=5） | 107.2 | 53.1 | 97.1 | 118.6 |
| en→ja | en→ja（opus-tatoeba-en-ja） | 10 | 14.59 | 12.31 | 1/5（20.0%） | -16.60（n=2） | 126.4 | 20.4 | 198.3 | 323.0 |
| ja→en | ja→en（opus-mt-ja-en） | 10 | 65.93 | 49.27 | 3/3（100.0%） | -12.33（n=2） | 0.0 | 34.7 | 45.8 | 45.8 |

### 分方向 × 分类

#### zh→en（route：zh→en（opus-mt-zh-en））

| 分类 | 条数 | 有参考 | chrF | BLEU | 专名保留率 | OCR chrF 差值 | P50（ms） | P95（ms） | max（ms） |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| short | 8 | 8 | 61.58 | 38.37 | 3/3（100.0%） | — | 35.3 | 50.3 | 51.8 |
| colloquial | 7 | 7 | 32.55 | 17.73 | 1/1（100.0%） | — | 29.8 | 37.4 | 39.3 |
| proper_noun | 6 | 6 | 64.24 | 17.27 | 9/13（69.2%） | — | 37.0 | 54.5 | 56.5 |
| ocr_noise | 6 | 6 | 59.27 | 31.48 | 1/4（25.0%） | -4.73（n=6） | 31.4 | 48.7 | 51.8 |
| paragraph | 4 | 4 | 52.10 | 30.20 | 1/3（33.3%） | — | 69.3 | 112.0 | 118.3 |

#### en→zh（route：en→zh（opus-mt-en-zh））

| 分类 | 条数 | 有参考 | chrF | BLEU | 专名保留率 | OCR chrF 差值 | P50（ms） | P95（ms） | max（ms） |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| short | 8 | 8 | 41.60 | 49.54 | 1/1（100.0%） | — | 18.3 | 23.8 | 24.3 |
| colloquial | 7 | 7 | 15.25 | 16.72 | — | — | 23.2 | 32.6 | 36.4 |
| proper_noun | 6 | 6 | 41.28 | 41.90 | 8/12（66.7%） | — | 36.1 | 48.8 | 52.1 |
| ocr_noise | 6 | 6 | 23.02 | 26.26 | 3/3（100.0%） | -3.83（n=6） | 29.6 | 56.6 | 60.1 |
| paragraph | 4 | 4 | 26.61 | 30.85 | — | — | 49.3 | 64.8 | 67.3 |

#### zh→ja（route：zh→ja（opus-mt-tc-big-zh-ja））

| 分类 | 条数 | 有参考 | chrF | BLEU | 专名保留率 | OCR chrF 差值 | P50（ms） | P95（ms） | max（ms） |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| short | 6 | 6 | 0.28 | 0.43 | — | — | 23.1 | 36.1 | 37.4 |
| colloquial | 5 | 5 | 1.96 | 1.85 | — | — | 28.6 | 77.2 | 87.5 |
| proper_noun | 5 | 5 | 1.74 | 1.16 | 0/9（0.0%） | — | 36.6 | 63.0 | 66.7 |
| ocr_noise | 5 | 5 | 0.97 | 0.91 | 0/2（0.0%） | +0.48（n=5） | 28.1 | 50.6 | 54.3 |
| paragraph | 4 | 4 | 2.11 | 0.52 | 0/1（0.0%） | — | 43.9 | 49.2 | 49.9 |

#### ja→zh（route：ja→en→zh（opus-mt-ja-en → opus-mt-en-zh））

| 分类 | 条数 | 有参考 | chrF | BLEU | 专名保留率 | OCR chrF 差值 | P50（ms） | P95（ms） | max（ms） |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| short | 6 | 6 | 23.22 | 27.72 | — | — | 42.4 | 59.8 | 63.3 |
| colloquial | 5 | 5 | 17.70 | 19.30 | — | — | 43.1 | 73.8 | 79.0 |
| proper_noun | 5 | 5 | 42.63 | 40.59 | 4/10（40.0%） | — | 61.5 | 111.6 | 118.6 |
| ocr_noise | 5 | 5 | 24.37 | 27.17 | 2/2（100.0%） | -0.16（n=5） | 43.2 | 65.6 | 68.7 |
| paragraph | 4 | 4 | 17.63 | 19.24 | — | — | 92.4 | 97.1 | 97.3 |

#### en→ja（route：en→ja（opus-tatoeba-en-ja））

| 分类 | 条数 | 有参考 | chrF | BLEU | 专名保留率 | OCR chrF 差值 | P50（ms） | P95（ms） | max（ms） |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| short | 3 | 3 | 24.73 | 30.52 | — | — | 20.2 | 22.3 | 22.5 |
| colloquial | 2 | 2 | 49.72 | 53.94 | — | — | 10.5 | 13.6 | 14.0 |
| proper_noun | 2 | 2 | 4.59 | 1.51 | 1/4（25.0%） | — | 179.8 | 308.7 | 323.0 |
| ocr_noise | 2 | 2 | 12.50 | 16.29 | — | -16.60（n=2） | 20.1 | 20.5 | 20.6 |
| paragraph | 1 | 1 | 14.32 | 14.31 | 0/1（0.0%） | — | 45.8 | 45.8 | 45.8 |

#### ja→en（route：ja→en（opus-mt-ja-en））

| 分类 | 条数 | 有参考 | chrF | BLEU | 专名保留率 | OCR chrF 差值 | P50（ms） | P95（ms） | max（ms） |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| short | 3 | 3 | 85.83 | 79.69 | — | — | 23.6 | 24.0 | 24.0 |
| colloquial | 2 | 2 | 28.87 | 12.04 | — | — | 27.8 | 44.0 | 45.8 |
| proper_noun | 2 | 2 | 86.15 | 61.87 | 3/3（100.0%） | — | 40.3 | 45.1 | 45.7 |
| ocr_noise | 2 | 2 | 54.21 | 25.98 | — | -12.33（n=2） | 37.0 | 39.3 | 39.6 |
| paragraph | 1 | 1 | 62.59 | 51.37 | — | — | 45.7 | 45.7 | 45.7 |

### 明显偏差

这些是对照译文后的例子，不是新的自动指标。参考译文仍是 draft，换一种说法也会拉低 chrF。

- **zh→ja 整向不可用。** chrF 1.61，专名 0/12。`请给我一杯水。` 译成 `本、`，`我明天休息。` 译成 `ドラゴン、`，`索尼总部位于东京。` 译成 `↓↓↓↓↓`。同一 revision 的 Hugging Face Marian 权重对 `请给我一杯水。` 同样生成 `本、`，CTranslate2 与上游一致。评测脚本没有改写译文。翻译核心本次不改，这个模型是否还能当 mvp 直连应另开 Issue。
- **en→ja 专名和幻觉。** `The shop opens at 10:30 in Shibuya.` 译成带 oricon URL 的长串；`Yuki` 变成 `雄基`，没有保留约定的 `ユキ`。该条把 P95 拉到 198 ms、max 323 ms。
- **en→zh 口语按字面翻。** `No cap, this is great.` 译成 `没有帽子,这是伟大的。`（chrF 5.3）。
- **zh→en 专名没按约定译法留下。** 北京大学译成 Beijing University，未保留 Peking University；李娜译成 Lena；OCR 噪声把 OCR 译成 `0CR`。
- **ja→zh 中转丢专名。** route 是 `ja→en→zh`。`富士山は静岡県と山梨県にまたがっています。` 译成 `"你在这里"和"山桥"`；索尼译成 `桑尼`。
- **ja→en 是六个方向里最好的。** chrF 65.93，专名 3/3。`マジで？` 译成 `Seriously?`，chrF 只有 9.9，是因为参考更口语，意思大体还在。

预热之后，多数方向的 P50 在 20–53 ms。五个模型都加载后的峰值 RSS 是 773.4 MiB。

## 已知限制

- 不改翻译核心，没有术语表或专名一致后处理。专名保留率只反映模型是否碰巧写出了约定译法。
- 参考译文仍是 `draft`，chrF / BLEU 会把合法的另一种译法算成错。
- 日文 BLEU 在没有可用的 `ja-mecab` 时是字符切分，和 MeCab 切分的 BLEU 不能直接比。
- 语料级 BLEU 对短句不稳定；主指标是 chrF。
- OCR 差值要求 `clean_id` 落在同一次评测、同一方向里。`--limit` 截掉干净句时，该条记为缺失，不计入差值。
- 加载耗时是进程内第一次 `preload` 的墙钟时间。六个方向共用已加载的模型，后面的方向不会重复计加载。
- 峰值 RSS 含解释器、全部已加载模型和 sacrebleu，不是单个模型的增量。
- 对照样例由 `--seed` 决定（默认 0），换种子会换那 5 条，汇总分数不变。
- 不做 COMET，也不在脚本里做人工评分。
